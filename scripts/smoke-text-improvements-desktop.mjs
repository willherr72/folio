// Isolated owned app only. FOLIO_DIALOG_MANUAL=1 lets a second shell submit dialogs.
import {chromium,expect} from '@playwright/test';
import {execFile} from 'node:child_process';import {promisify} from 'node:util';
import {existsSync,readFileSync,writeFileSync,renameSync} from 'node:fs';import{resolve}from'node:path';import{createHash}from'node:crypto';
const exec=promisify(execFile),pid=process.env.FOLIO_APP_PID;if(!pid||!/^\d+$/.test(pid))throw Error('Set owned FOLIO_APP_PID');
const dir=resolve('artifacts/text-edit-desktop'),original=resolve(dir,'Original.pdf'),saved=resolve(dir,'Folio edited.pdf');
if(existsSync(saved)&&process.env.FOLIO_TEXT_EDIT_RESUME!=='saved')throw Error('Expected a fresh saved output');
const hash=path=>createHash('sha256').update(readFileSync(path)).digest('hex'),originalHash=hash(original);
const browser=await chromium.connectOverCDP(process.env.FOLIO_CDP_URL||'http://127.0.0.1:9245');const page=browser.contexts()[0].pages().find(p=>!p.url().startsWith('devtools:')),errors=[];page.on('pageerror',e=>errors.push(e.message));
const canvas=()=>page.locator('.document-page').first().locator('.page-canvas');
async function dialog(action,path){console.log('DIALOG '+action+' '+path);if(!process.env.FOLIO_DIALOG_MANUAL)await exec('pwsh',['-NoProfile','-File',resolve('scripts/set-native-dialog.ps1'),'-AppProcessId',pid,'-FilePath',path,'-Action',action],{windowsHide:true,timeout:60000});}
async function open(path,name){await page.getByRole('button',{name:/^(Open|Open a PDF)$/}).click();await dialog('Open',path);await expect(page.getByRole('tab',{name,exact:true})).toHaveAttribute('aria-selected','true',{timeout:180000});await expect(canvas().locator('image')).toHaveCount(1,{timeout:30000});}
async function sourceText(){return canvas().locator('.pdf-text-layer').textContent();}
try{
 const resume=process.env.FOLIO_TEXT_EDIT_RESUME==='overflow', resumeSave=['save','saved'].includes(process.env.FOLIO_TEXT_EDIT_RESUME);
 if(!resumeSave){
 if(!resume){
 await open(original,'Original.pdf');
 await page.getByRole('button',{name:'Text',exact:true}).click();await canvas().click({position:{x:110,y:250}});await page.getByRole('textbox',{name:'Content',exact:true}).fill('Kept addition');await page.getByRole('combobox',{name:'Font',exact:true}).selectOption('Courier-BoldOblique');await expect(canvas().locator('[data-overlay] text')).toHaveAttribute('font-style','italic');
 await page.getByRole('button',{name:'Edit text',exact:true}).click();await page.getByRole('button',{name:'Edit text: Original sentence',exact:true}).click();
 }
 const replacement=page.getByRole('textbox',{name:'Replacement text',exact:true});if(!resume){await expect(replacement).toBeFocused();await replacement.fill('This replacement is far too long to fit in the original space');await page.getByRole('button',{name:'Apply changes',exact:true}).click();}await expect(page.getByRole('dialog').getByRole('alert')).toContainText(/fit|space|width|wide|edge|outside|overlap/i,{timeout:30000});
 await expect(replacement).toHaveValue('This replacement is far too long to fit in the original space');await replacement.fill('New sentence with more detail');await page.getByRole('button',{name:'Apply changes',exact:true}).click();await expect(page.getByRole('dialog')).toHaveCount(0,{timeout:30000});await expect.poll(sourceText).toContain('New sentence with more detail');expect(await sourceText()).not.toContain('Original sentence');await expect(canvas().locator('[data-overlay]')).toContainText('Kept addition');
 await page.getByRole('button',{name:'Undo',exact:true}).click();await expect.poll(sourceText).toContain('Original sentence');await page.getByRole('button',{name:'Redo',exact:true}).click();await expect.poll(sourceText).toContain('New sentence with more detail');
 await page.getByRole('button',{name:'Find',exact:true}).click();await page.getByRole('searchbox').fill('Original sentence');await expect(page.locator('.search-count')).toContainText('No matches');await page.getByRole('searchbox').fill('New sentence with more detail');await expect(page.locator('.search-count')).toContainText('1 of 1');
 await page.getByRole('button',{name:'Close search',exact:true}).click();
 const selection=await page.evaluate(()=>{const nodes=[...document.querySelectorAll('.document-page .pdf-text-layer [data-pdf-character]')];const text=nodes.map(n=>n.textContent).join(''),start=text.indexOf('New sentence with more detail');const rects=[nodes[start].getBoundingClientRect(),nodes[start+28].getBoundingClientRect()];window.__lastCopy=null;window.__captureCopy=e=>{window.__lastCopy=e.clipboardData.getData('text/plain');};window.addEventListener('copy',window.__captureCopy);return rects.map(r=>({x:r.x,y:r.y,width:r.width,height:r.height}));});
 await page.mouse.move(selection[0].x+selection[0].width*.25,selection[0].y+selection[0].height/2);await page.mouse.down();await page.mouse.move(selection[1].x+selection[1].width*.75,selection[1].y+selection[1].height/2,{steps:12});await page.mouse.up();await page.keyboard.press('Control+c');await expect.poll(()=>page.evaluate(()=>window.__lastCopy)).toBe('New sentence with more detail');
 await page.getByRole('button',{name:'Save a copy',exact:true}).click();}
 if(process.env.FOLIO_TEXT_EDIT_RESUME!=='saved')await dialog('Save',saved);await expect.poll(()=>existsSync(saved),{timeout:180000}).toBe(true);await expect(page.locator('.statusbar')).toContainText('Saved a copy');expect(hash(original)).toBe(originalHash);await page.screenshot({path:resolve(dir,'edited.png')});
 await page.getByRole('button',{name:'Close Original.pdf',exact:true}).click();renameSync(original,resolve(dir,'Moved original.pdf'));
 await open(saved,'Folio edited.pdf');await expect.poll(sourceText).toContain('New sentence with more detail');expect(await sourceText()).not.toContain('Original sentence');await expect(canvas().locator('[data-overlay]')).toContainText('Kept addition');await expect(canvas().locator('[data-overlay] text')).toHaveAttribute('font-family','"Courier New", Courier, monospace');await expect(canvas().locator('[data-overlay] text')).toHaveAttribute('font-weight','700');await expect(canvas().locator('[data-overlay] text')).toHaveAttribute('font-style','italic');await page.getByRole('button',{name:'Edit text',exact:true}).click();await expect(page.getByRole('button',{name:'Edit text: New sentence with more detail',exact:true})).toBeVisible();
 await open(resolve(dir,'Unsupported.pdf'),'Unsupported.pdf');await page.getByRole('button',{name:'Edit text',exact:true}).click();await page.getByRole('button',{name:'Inspect unsupported text: Original sentence',exact:true}).click();await expect(page.getByRole('dialog')).toContainText(/embedded|custom|encoding|font/i);await expect(page.getByRole('button',{name:'Apply changes',exact:true})).toHaveCount(0);await page.getByRole('button',{name:'Cancel',exact:true}).click();expect(errors).toEqual([]);
 writeFileSync(resolve(dir,'results.json'),JSON.stringify({passed:true,resumedFromOverflow:resume,resumedAtSave:resumeSave,originalSha256:originalHash,outputSha256:hash(saved),checks:['overflow rejection preserves input','longer actual content replacement','Courier bold italic annotation preserved','undo/redo','search invalidation','native mouse selection and Ctrl+C','original unchanged','saved/reopened without original path','reopened run editable','embedded-font refusal'],errors},null,2));console.log('PASS existing text desktop');
}finally{await browser.close();}
