{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Core capability for the Folio editor window",
  "windows": ["main"],
  "permissions": ["core:default"]
}